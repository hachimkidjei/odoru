server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/odoru_member_db?sslmode=disable
    username: odoru
    password: odoru
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  health:
    db:
      enabled: false