server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://member-postgres:5432/odoru_member_db
    username: odoru
    password: odoru
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  health:
    db:
      enabled: false